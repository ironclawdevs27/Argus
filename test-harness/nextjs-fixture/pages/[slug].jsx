// Dynamic route fixture — Argus C3 harness
// This file should NOT be included in discovered routes (dynamic param, no crawlable URL).
export default function SlugPage({ slug }) {
  return <div>{slug}</div>;
}
